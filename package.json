{
  "name": "website-kci",
  "version": "1.0.0",
  "private": true,
  "description": "KCI website rewrite monorepo with staged JS migration",
  "workspaces": [
    "apps/*",
    "packages/*",
    "scripts/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace apps-api run dev",
    "build": "npm run build --workspaces",
    "lint": "echo 'TODO: add lint step'"
  }
}
